# include<iostream.h>
# include<conio.h>
void main()
{
    clrscr();
    int a,b;
    
    cout<<"Enter 2 numbers to know, the greater of the two."<<endl;
    cin<<a<<b;

    if(a>b);
    cout<<a<<"is the greater number.";
    else if(b>a);
    cout<<b<<"is the greater number.";
    else
    cout<<"Both the numbers are equal.";
    _
    getch();
}